# Grab these from your browser devtools while logged into
# https://registration9.uc.cl/StudentRegistrationSsb/ssb/classSearch/classSearch
# Network tab -> any request to registration9.uc.cl -> Cookies / Headers.
# They expire (hours, not days) -- when searches start failing, refresh these.

UC_JSESSIONID=
UC_CF_CLEARANCE=
UC_QUEUEIT_COOKIE_NAME=QueueITAccepted-SDFrts345E-V3_ucclprod01incrip
UC_QUEUEIT_COOKIE_VALUE=
UC_RBDI_COOKIE=
UC_SYNCHRONIZER_TOKEN=

# comma separated origins allowed to call this backend
CORS_ORIGIN=http://localhost:5173

PORT=8787
